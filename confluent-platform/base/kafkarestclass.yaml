apiVersion: platform.confluent.io/v1beta1
kind: KafkaRestClass
metadata:
  name: default
  namespace: confluent
spec:
  kafkaRest:
    kafkaClusterRef:
      name: kafka
    kafkaRest:
      authentication:
        bearer:
          secretRef: rest-credential
        type: bearer
      tls:
        enabled: true