apiVersion: platform.confluent.io/v1beta1
kind: KafkaRestClass
metadata:
  name: default
  namespace: confluent
spec:
  kafkaRest:
    endpoint: https://kafka.confluent.svc.cluster.local:8090
    tls:
      enabled: true